services:
  frontend:
    build: ../../frontend
    ports:
      - "2067:80"
    environment:
      IDENTITY_URL: ${IDENTITY_URL:-}
      AGGREGATOR_URL: ${AGGREGATOR_URL:-}
    depends_on:
      - aggregator
      - identity
    restart: unless-stopped